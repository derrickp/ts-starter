{
  "name": "ts-starter",
  "devDependencies": {
    "@types/jest": "^26.0.7",
    "@yarnpkg/pnpify": "^2.1.0",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.3",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "compile": "yarn tsc -v && yarn tsc",
    "test": "yarn jest"
  }
}
